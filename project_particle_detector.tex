\documentclass{article}

% Language setting
% Replace `english' with e.g. `spanish' to change the document language
\usepackage[english]{babel}

% Set page size and margins
% Replace `letterpaper' with `a4paper' for UK/EU standard size
\usepackage[letterpaper,top=2cm,bottom=2cm,left=3cm,right=3cm,marginparwidth=1.75cm]{geometry}

% Useful packages
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage[colorlinks=true, allcolors=blue]{hyperref}

% Packages I've added.
\usepackage{listings}
\usepackage{float}
\usepackage{xcolor}
\usepackage{comment}

\setlength{\parskip}{0.8em}
\setlength{\parindent}{0pt}

\renewcommand{\lstlistingname}{Code snippet}

\lstdefinestyle{vscode}{
    language=C++,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue},
    commentstyle=\color{green!50!black},
    stringstyle=\color{orange},
    numbers=left,
    numberstyle=\tiny\color{gray},
    stepnumber=1,
    numbersep=8pt,
    backgroundcolor=\color{gray!10},
    frame=single,
    breaklines=true,
    captionpos=b,
    tabsize=4,
    showstringspaces=false
}

\title{Assignment 5 report}
\author{Callum Williamson}

\begin{document}
\maketitle

\begin{abstract}
    This is my placeholder abstract.
\end{abstract}

\section{Introduction}

This report includes design considerations for the particle detector final project and a description of the software built.

Git pushes for this report will have been done regularly throughout.
Also please note, the design considerations and process will be written as I go along and then refined later, so as to see a clear history and to align with my code commits.
Due to this, the initial English used in my earlier commits may not be the best and is more note like.

\section{Design process \& considerations}

The design process is discussed in two stages: the implementation of the minimum functionality, followed by the reasoning behind the advanced features.
Both sections are structured around the main components of the program.
First, the development of the \texttt{Particle} class hierarchy is discussed, followed by the development of the \texttt{SubDetector} class hierarchy. Finally, the construction of the \texttt{DetectorSystem} class is described.

\subsection{Minimum functionality}

The minimum functionality of this project is paraphrased from the project slides below:
\begin{itemize}
    \item Pure abstract base classes for a sub-detector and particle.
    
    \item Derived classes for sub-detectors: trackers, calorimeters and muon chambers.
    
    \item Derived classes for sub-detectors: electrons, muons, photons and hadrons.

    \item Virtual functions to set and get characteristics of the detectors and particles.

    \item Four-momentum is stored as a class in each particle.

    \item A detector that returns the energy of the particle it detected.

    \item A generic detector class that stores sub-detectors.    
\end{itemize}
As we move through this section, the approach of how these were met is discussed.
\textcolor{red}{The requirement of setters and getters being virtual is though on with its necessity questioned.}

\subsubsection{Particles}

Much of the \texttt{Particle} architecture used in this project was initially developed in Assignments 4 and 5.
The discussion is therefore split into the prior design decisions that formed the starting point for the project and the present development carried out for this project.
\textcolor{red}{Should I put the repo link?}

\textcolor{red}{Skip the prior for now.}
\textcolor{red}{Mention abstract base class, four momentum and setters/getters}

The responsibilities previously held by Assignment 5's \texttt{Lepton} class were moved to this project's \texttt{Particle} base class, now extended to not just leptons.
To begin with, a simple \texttt{Electron} class was directly derived from \texttt{Particle}, without retaining the intermediate \texttt{ChargedLepton} layer used in Assignment 5. This simplified structure was chosen to test the initial interaction between the \texttt{Particle} and \texttt{SubDetector} classes before introducing a more complex class hierarchy.

The information regarding which \texttt{SubDetector} classes a derived \texttt{Particle} interacts with is stored in the \texttt{Particle} hierarchy rather than the \texttt{SubDetector} hierarchy.
This design choice was made for several considerations.
First, many interaction properties are shared by similar particles. Storing this information in the \texttt{Particle} hierarchy allows for common behaviour to be encoded through inheritance, reducing code repetition.
For example, \textcolor{red}{as shown in Code snippet \ref{lst:interacts_with_tracker}}, the data member \texttt{charge\_mag} determines whether a \texttt{Particle} interacts with a \texttt{Tracker}.
Since this logic/behaviour is the same for all particles it can be defined once in the base class rather than repeatedly implemented in each derived class.
This design also supports future extension. If a new derived \texttt{Particle} is added, its interaction behaviour may already be defined and if not just needs introducing in that derived class implementation.
\textcolor{red}{An example of where a particle's behaviour is already defined is the case in which a new derived \texttt{Hadron} is created.}
In contrast, if the information was stored in the \texttt{SubDetector} hierarchy, each \texttt{SubDetector} derived class would have to be updated whenever a new \texttt{Particle} was introduced.
Note, it is also important to distinguish between whether a \texttt{Particle} can interact with a \texttt{SubDetector} and whether it is actually detected. The \texttt{interacts\_with} member functions define whether detection is physically possible for a given \texttt{Particle}.
\texttt{SubDetector} specific conditions, such as whether a \texttt{SubDetector} is switched on through its \texttt{status} data member, are still handled by the \texttt{SubDetector} classes.
This keeps the particle interaction logic separate from the detector measurement logic, improving both flexibility and clarity.

\textcolor{red}{Maybe too much and need to keep it lighter. But all true.}
\begin{lstlisting}[
    style=vscode, 
    caption={
    \texttt{Particle} member function \texttt{interacts\_with\_tracker}. Only \texttt{interacts\_with} member function defined in \texttt{Particle} and not overridden in the derived classes.},
    label={lst:interacts_with_tracker}
]
bool Particle::interacts_with_tracker() const
{
    if (charge_mag == 0)
    {
        return false; // Neutral Particles don't interact with Trackers.
    }
    else
    {
        return true; // Charged Particles interact with Trackers.
    }
}
\end{lstlisting}
\texttt{Particle} member functions, with the name beginning with \texttt{interacts\_with}, were chosen to handle \texttt{Particle}/\texttt{SubDetector} interaction instead of another idea, a \texttt{vector} data member that stores a list of \texttt{string} or \texttt{enum} data types.
This decision was first based on the fact that it allowed for simpler inheritance. Take for example the case of the \texttt{Hadron} class, like all \texttt{Particle} classes it inherited its \texttt{Tracker} behaviour from Code snippet \ref{lst:interacts_with_tracker} and for the other behaviour, it was shared and hence was defined in \texttt{Hadron} and inherited by the derived and intermediate base classes below it.
This is simpler and clearer than defining a \texttt{vector} data member that is shared between derived classes.
Such a \texttt{vector} would store the result of the interaction logic, but would not express the logic itself as clearly as a dedicated member function as shown in Code snippet \ref{lst:interacts_with_tracker}.
The member function approach also improves adaptability, where the \texttt{vector} limits it. For example, if future development required \texttt{Muon} objects below a certain energy to not  interact with \texttt{MuonChamber} objects, this behaviour could be implemented naturally inside the relevant member function.
This design choice also makes \texttt{SubDetector} structure more straightforward. Each \texttt{SubDetector} only needs to call a single \texttt{bool} member function, rather than iterating through a \texttt{vector} to check whether the \texttt{Particle} interacts with it.
\textcolor{red}{Maybe mention possible negatives: that a vector format could be got from a data file or that it may be simpler to introduce a new SubDetector as you only need to write one more word in each particles vector. However one could argue this would take longer than mine as I have inheritance built in}

Upon reaching the minimum functionality for the \texttt{SubDetector} and \texttt{DetectorSystem} the class structures for \texttt{Muon}, \texttt{Photon} and \texttt{Hadron} were created.

As shown in Figure \ref{fig:Particle_UML}, the \texttt{Photon} class was derived directly from \texttt{Particle}.
This was because it has distinct physical properties and does not share sufficient common behaviour with the other particles required for minimum functionality to justify an intermediate base class.
It became clear in \texttt{Photon} that the inherited data members \texttt{rest\_mass} and \texttt{is\_anti} were a bit redundant as it has no mass and is its own anti particle.
However, they are not damaging and a necessity for the majority of the \texttt{Particle} classes. Therefore it was worthwhile and safe to keep them as base data members.
The base member function \texttt{print\_info}, which should print the characteristics common to all \texttt{Particle} objects did seem to be omitting information.
Rather than removing it, I added a statement to the \texttt{Photon} classes' polymorphic \texttt{Photon::print\_info} regarding the fact that \texttt{Photon} objects are their own anti-particles. 
The same was done for neutral the \texttt{Pion} \textcolor{red}{(and \texttt{Rho})}.

Since hadronic particles share similar properties and behaviour, they were organised under the intermediate base class \texttt{Hadron}.
A subsequent intermediate base class \texttt{Meson} was introduced, as they follow common charge dependent construction logic for the neutral and charged states. This inheritance reduced repetition across the derived \texttt{Meson} classes and makes the hierarchy clearer, as shown in Figure \ref{fig:Particle_UML}.

\texttt{Muon} was developed

\begin{lstlisting}[
    style=vscode, 
    caption={
    Code snippet acting as placeholder for Particle UML diagram},
    label={fig:Particle_UML}
]


\end{lstlisting}

\subsection{Detectors}

\subsubsection{SubDetector}

With the detectors, my initial plan is to have a pure abstract base class \texttt{SubDetector} from which \texttt{Tracker}, \texttt{Calorimeter} and \texttt{MuonChamber} etc can be derived.
Then have a the generic overall detector \texttt{DetectorSystem} that stores the \texttt{SubDetector}s in a vector like.

Should \texttt{SubDetector} change the Particle in anyway, what if the detection changes the energy due to a collision, something to consider.

I don't believe any need for specific copy/move constructors/assignment operators as the data members are only \texttt{std::string} and \texttt{bool}.

Considering how to control if a \texttt{SubDetector} detects a \texttt{Particle} passed through it.
My two current ideas are using a \texttt{Particle} distinct data member that is a vector with strings, referring to which \texttt{SubDetectors} can detect this \texttt{Particle}. Then in the derived \texttt{SubDetector} member function \texttt{detect} it would use an if statement distinguishing if the \texttt{Particle} passed through can be detected.

The other method I was considering was using a polymorphic virtual \texttt{Particle} member function, for example \texttt{interacts\_with\_calorimeter}, which will return a bool depending on if that derived \texttt{Particle} interacts with that detector.
Then in the derived \texttt{SubDetector} member function \texttt{detect}, \texttt{particle.interacts\_with\_calorimeter} can be called which will just return true or false.

I find this method cleaner as no loop through a vector of strings or enums is needed in the \texttt{detect}. It also allows for flexibility, in the sense perhaps \texttt{Electron} only interacts with \texttt{Calorimeter} when it has certain energies.

Noted that \texttt{detect} for all \texttt{SubDetector} should first have their \texttt{status} checked as if it is off then nothing should be detected.
As this is common to all \texttt{SubDetector} I initially thought to make it within the base \texttt{detect}. However, calling this function within the derived \texttt{detect} could not leave the derived function using \texttt{return} and stop the detection process. But I still did not like doing this for each derived class
\begin{lstlisting}[
    style=vscode, 
    caption={Example of repetition for status check in derived SubDetectors},
    label={lst:repetition_status}
]
if (!status)
{
    std::cout << "Calorimeter (" << sd_name << ") is off, no detection." << std::endl;
    std::cout << std::endl; // Line break.
    return; // Exit function.
}
\end{lstlisting}
My solution was to make a base member function \texttt{is\_off}, as seen in Code snippet \ref{lst:repetition_status}, which prints the information required (the same for all \texttt{SubDetector}) and returns true if off and false if on.
\begin{lstlisting}[
    style=vscode, 
    caption={Base function to check status, called in derived class detect.},
    label={lst:is_off}
    ]
bool SubDetector::is_off() const
{
    if (!status)
    {
        std::cout << "SubDetector (" << sd_name << ") is off. No detection." << std::endl;
        std::cout << std::endl; // Line break.
        return true;
    }
    else
    {
        return false;
    }
}
\end{lstlisting}
This is then used in the derived \texttt{detect} function, being used to exit the function prior to detection. 


Also detect should not necessarily keep \texttt{Particle} constant as the detection may change some of the kinematics something to note??

Should I make \texttt{Tracker} dependent on charge. This goes to the discussion as to whether have charge as base data member.

I am going to make a Hadronic and Electromagnetic Calorimeter class derived from Calorimeter as they interact with different particles as shown from the CMS detector. They will just use different member functions for \texttt{interacts\_with}.
Actually I have decided not to have two different \texttt{detect} functions for the derived functions, as would have been like Code snippet  \ref{lst:two_calorimeter_detects}.
\begin{lstlisting}[
    style=vscode, 
    caption={Previous data member function for \texttt{HadronCalorimeter}, which would have had code repetition.},
    label={lst:two_calorimeter_detects}
    ]
    // Function for HadronCalorimeter detection of Particle class.
void HadronCalorimeter::detect(const Particle& particle) const
{
    // Check status of SubDetector.
    if (is_off())
    {
        return; // Exit function.
    }

    if (particle.interacts_with_hadron_calorimeter())
    {
        std::cout << "Calorimeter (" << sd_name << ") measures the energy of the particle as: " << particle.get_four_momentum().get_E() << " MeV" << std::endl;
        // Will differentiate these later.
        std::cout << "The particle's true energy is: " << particle.get_four_momentum().get_E() << " MeV" << std::endl;
        std::cout << std::endl; // Line break.
    }
    else
    {
        std::cout << "Particle does not interact with the calorimeter (" << sd_name << "), no energy measurement." << std::endl;
        std::cout << std::endl; // Line break.
    }
}
\end{lstlisting}

% Instead have it central to the base intermediate \texttt{Calorimeter}, as shown in Code snippet \ref{lst:efficient_calor_detect} , by introducing a virtual member function \texttt{particle\_interacts} which is overridden in the derived classes. It acts as a switch between the two \texttt{interacts\_with} functions. It also means only one line is changed in \texttt{Calorimeter::detect} to allow for the derived detection.
\begin{lstlisting}[
    style=vscode, 
    caption={\texttt{Calorimeter::detect} function that means it does not need to be overridden in each derived class, which would cause code repetition. This is facilitate via the more simple \texttt{particle\_interacts} virtual function which is overidden in the derived classes.},
    label={lst:efficient_calor_detect}
    ]
// Function for HadronCalorimeter detection of Particle class.
void HadronCalorimeter::detect(const Particle& particle) const
{
    // Check status of SubDetector.
    if (is_off())
    {
        return; // Exit function.
    }

    if (particle.interacts_with_hadron_calorimeter())
    {
        std::cout << "Calorimeter (" << sd_name << ") measures the energy of the particle as: " << particle.get_four_momentum().get_E() << " MeV" << std::endl;
        // Will differentiate these later.
        std::cout << "The particle's true energy is: " << particle.get_four_momentum().get_E() << " MeV" << std::endl;
        std::cout << std::endl; // Line break.
    }
    else
    {
        std::cout << "Particle does not interact with the calorimeter (" << sd_name << "), no energy measurement." << std::endl;
        std::cout << std::endl; // Line break.
    }
}
\end{lstlisting}



Also considering that perhaps a vector of what a particle interacts with is better. As if this possessed a lot of information it could be got from a dataset. With mine it is built into a data member function, on the otherhand it is set in stone what \texttt{Particle} a \texttt{SubDetector} can detect.



\subsubsection{DetectorSystem}

Now will create the class that can store multiple \texttt{SubDetector} classes to match the likes of \textcolor{red}{ATLAS or CMS} and print the overall configuration. This will be a container class.
My plan is to use pointers and vectors to control ownership of the multiple \texttt{SubDetector} classe, via having the data member shown in Code snippet \ref{lst:container_pointer}.
\begin{lstlisting}[
    style=vscode, 
    caption={Data member for \texttt{DetectorSystem} to manage the ownership of multiple \texttt{SubDetector} classes},
    label={lst:container_pointer}
    ]
    private:
    std::vector<std::unique_ptr<SubDetector>> sub_detectors;
\end{lstlisting}

Do I have a getter for the data member in Code snippet \ref{lst:container_pointer}? I think not but have a function that prints the configuration.

Perhaps may want to use \texttt{static} to keep track of detector names so that there are no repeats. Or like in assignment 2, they could have ID that cannot repeat.

Currently using same \texttt{Debug} namespace to decide on whether messages for adding \texttt{SubDetector} classes to \texttt{DetectorSystem}, however may change this to allow for more flexibility.
Decided to use namespace \texttt{DSMessages} (Detector System Messages) to help add this flexibility.

Below is also beyond minimum functionality as the brief only asks to be able to add. I decided a remove function and a release which lets you receive the SubDetector you are removing.

For \texttt{remove\_sub\_detector} and \texttt{release\_sub\_detector} ChatGPT 5.2 suggested using \texttt{std::find\_if} and a \textcolor{red}{lambda function} to search through the vector containing the \texttt{SubDetector} classes.
\begin{lstlisting}[
    style=vscode, 
    caption={\texttt{remove\_sub\_detector} member function for \texttt{DetectorSystem}.},
    label={lst:remove_sub_detector}
    ]
void DetectorSystem::remove_sub_detector(const std::string& rem_sub_detector_name)
{
    std::vector<std::unique_ptr<SubDetector>>::iterator it = std::find_if(
        sub_detectors.begin(),
        sub_detectors.end(),
        [&rem_sub_detector_name](const std::unique_ptr<SubDetector>& sub_detector)
        {
            return sub_detector->get_sd_name() == rem_sub_detector_name;
        }
    );

    if (it == sub_detectors.end())
    {
        std::cerr << "Error: SubDetector (" << rem_sub_detector_name << ") not found in DetectorSystem (" << sys_name << ")." << std::endl;
        return;
    }

    sub_detectors.erase(it);
    if (DSMessages::show_messages)
    {
        std::cout << "Removed SubDetector (" << rem_sub_detector_name << ") from DetectorSystem (" << sys_name << ")." << std::endl;
    }
}
\end{lstlisting}
I made it like this with the \textcolor{red}{lambda function} to maintain simplicity. The namespace \texttt{DSMessages} also lets the user controller if they want to see the remove messages.
Should note that the error messages is mandatory no matter \texttt{Debug} or \texttt{DSMessages}.

\texttt{DetectorSystem::detect} works well, even for the SubDetector's status. But it made me realise I want to develop a way to alter the SubDetectors contained in a DetectorSystem. \textcolor{red}{This is likely additional features.} I did this making \texttt{get\_sub\_detector} which is used within a wrapper \texttt{DetectorSystem::set\_sub\_detector\_status}.




\subsection{Particles}

Note that the distinct data members from assignment 5 for the derived \texttt{Particle} are not really useful, however will keep for now.

For now assuming \texttt{Muon} doesn't interact with \texttt{Calorimeter}.

Now having done the minimum functionality for the \texttt{DetectorSystem} class I will come and make the required derived \texttt{Particle} classes: \texttt{Photon} and \texttt{Hadron}.

When making the \texttt{Photon} class the inherited data members \texttt{rest\_mass} and \texttt{is\_anti} are quite redundant as they have 0 mass and they are their own antiparticles. However I still think it is worth them being derived from \texttt{Particle} as these redundancies are not damaging.
Perhaps can remove the anti part from \texttt{Photon::print\_info}.

For \texttt{Hadron} there are many with different properties, so this will likely have to be an intermediate base class. For now not going to invest time when I am not sure.

When considering the Hadron mesons I am considering that they are different enough from the Baryons as they will have to deal with the fact that a pion for example can be charge +1, -1 (these are each others antiparticles) and 0 charge which is its own antiparticle.
Kaons have the same structure but their neutral also has an anti particle. Hence their anti and charge structures are going to have to be intertwined.
This also brings the question as to whether my \texttt{Particle} should have a charge data member and how it links to the \texttt{is\_anti} status.
Prior to making an intermediate hadron class and then maybe subsequent meson and baryon classes. I am going to make pion and proton directly. That way I can see what is shared and what is required. Then can adjust accordingly.
\texttt{Pion} detection by tracker depends on its charge, I think here we can have \texttt{bool} that if true it means it is a charged pion and hence it has an anti and if 0 it is a neutral pion and hence doesnt have an anti.
Note, that a charged \texttt{Pion} is slightly heavier than a neutral \texttt{Pion}.
It is also making me consider a charge magnitude base data member. This could be an argument for mesons but not for electrons.
The member function \texttt{interacts\_with\_tracker} could then be fully dependent on this. It would have to be different than just a \texttt{bool} for \texttt{is\_charged}. It would have to cater for a charge magnitude of 2 for example.

Mention that we chose \texttt{Muon} to not be detected by any \texttt{Calorimeter} and say why?

I think \texttt{Hadron} intermediate base class makes sense as all of them interact with the \texttt{HadronCalorimeter} and this will limit code repetition. They also all share the same interaction with the other \texttt{SubDetector} classes.

If I am going to have charge magnitude as a data member do I need validation. No as user doesnt set it.

Will define \texttt{interacts\_with\_tracker} in \texttt{Particle} as it is the same for all, it depends if it has \texttt{charge\_mag}.

If using \texttt{charge\_mag} and \texttt{is\_anti} to print the charge, this will have to be particle specific as some anti particles are +1 some are -1.

In \texttt{Pion} I need to link the \texttt{is\_charged} to the anti, so that anti is either the positive or negative pion and hence if the user makes changes to \texttt{is\_anti} it changes the charge +ve or -ve. I have defined the negative \texttt{Pion} as the anti particle.

Now using the logic which I used in \texttt{Pion} that is useful for all mesons in the \texttt{Meson} intermediate base class from which the mesons will (\texttt{Pion}) will be derived. This logic consists of having a data member \texttt{is\_charged} and using it to decide how charge is printed and how the \texttt{rest\_mass} is set, as the \texttt{Meson} neutral particle's mass differs from the charged mass.
As shown in Code snippet \ref{lst:prev_pion_constructor}, right now (previously)  my \texttt{Pion} parametrised constructor uses \texttt{is\_charged} to determine the \texttt{rest\_mass}.
\begin{lstlisting}[
    style=vscode, 
    caption={Previous \texttt{Pion} parameterised constructor.},
    label={lst:prev_pion_constructor}
    ]
Pion::Pion(double E, double px, double py, double pz, bool is_anti, bool is_charged)
    : Hadron(E, px, py, pz, is_charged ? 139.57 : 134.98, is_anti, is_charged ? 1 : 0), // Calling the intermediatebase class parameterised constructor.
    is_charged(is_charged) // rest_mass is set based on if charged or neutral, so is the charge_mag
{
    if (Debug::show_messages)
    {
        std::cout << "Calling Pion Parameterised Constructor." << std::endl;
    }
}
\end{lstlisting}
I need to move this logic so it is the same for all \texttt{Meson} classes and hence reduces code repetition.

All mesons are -1, 0, +1 charge, nothing greater.

\textcolor{red}{Current question aswell, should the user be able to change the \texttt{Meson} classes' \texttt{is\_charged}?}

\textcolor{red}{Need to ensure that the intermediate particles cannot be constructed and that especially their defaults}

In \texttt{Meson} neutral and charged rest masses are not stored as data members as they're not needed after initialisation.
I think this is a good design choice but we'll see if it brings about mistakes.

I believe the only differentiation for the \texttt{Meson} derived classes will be their masses and  that I'll print that the \texttt{Pion} neutral is its own anti-particle and this wont be printed for example for \texttt{Kaon}.

\textcolor{red}{Do need to consider that the \texttt{Meson} neutral particles may be more unstable and hence behave differently with detectors.}

Introduction of the base data member \texttt{charge\_mag} mean that I need to adjust all other \texttt{Particle} derived classes.

Tracker is now charge dependent and defined in \texttt{Particle} as it is the same logic for all. This reduces code repetition.

Also may make \texttt{Charged\_Lepton} class as \texttt{Electron} and \texttt{Muon} share \texttt{charge\_mag} and how charge is printed.

\textcolor{red}{Should I have a separate folder for intermediate base classes.}


\subsection{Misc}

Note that namespace was used, an advanced C++ to help with debugging and code exiting. 

Make sure all code snippets are up to date and dont have any of that differentiate later.

Unsure if my base classes are PURE abstract. 

Check masses are all to same sig fig.

I think there is no need for an intermediate baryon intermediate base class as right now \texttt{Proton} gets everything from \texttt{Hadron}.

Discussion of why my setters and getters are not virtual.

Is making the detectors unique in what they detect min functionality.

Do my \texttt{SubDetector} classes need to need to read the \texttt{FourMomentum}. 

However there maybe for \texttt{Meson} as there structure of having neutral and charged is similar.

\textcolor{red}{Check code snippets are indented properly}

\textcolor{red}{Should I reference where I found the masses}

\textcolor{red}{Should I validate the SubDetector naming??}

\textcolor{red}{I also note that I may want better error and exit logic.}

\section{Results}

\section{Discussion}

\section{Conclusions}

\end{document}